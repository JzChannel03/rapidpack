import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primaryRed = Colors.red;
  static const Color darkRed = Color(0xFFC62828); // For NavBar / dark accents
  
  // Backgrounds & Surfaces
  static const Color background = Colors.white;
  static const Color surfaceGray = Color(0xFFF5F5F5); // Light gray for pills/backgrounds
  static const Color borderGray = Color(0xFFEAEAEA); // Card borders
  static const Color textDark = Color(0xFF1A1A1A); // Primary body text
  static const Color textMuted = Color(0xFF999999); // Subtitles / hint text
  static const Color textGray = Color(0xFF666666); // Explanation dialog text
  
  // Accent & Alerts
  static const Color accentBlue = Color(0xFF4FC3F7); // Expected dates accent
  static const Color alertAmber = Colors.amber; // Warning alerts
  static const Color lightRedBg = Color(0xFFFFF0EE); // Selected options / card icon backgrounds
}
